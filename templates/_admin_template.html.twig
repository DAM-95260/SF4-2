{% extends "base.html.twig" %}


{% block title %}
    
{% endblock %}
{% block body %}
    {# Menu qui sera affiché sur toutes les pages du Back-Office #}
    <nav class="navbar has-background-primary is-dark">
        <div class="navbar-brand">
            <a class="navbar-item" href="{{ path('home') }}">
                <p class="subtitle is-4 ">
                    <i class="far fa-arrow-alt-circle-left"></i>
                </p>
            </a>

            <a role="button" class="navbar-burger burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
                <span aria-hidden="true"></span>
                <span aria-hidden="true"></span>
                <span aria-hidden="true"></span>
            </a>
        </div>

        <div id="navbarBasicExample" class="navbar-menu">
            <div class="navbar-start">
                <a class="navbar-item" href="{{ path('admin_product_list') }}">
                    Produits
                </a>
            </div>

            <div class="navbar-end">
                <div class="navbar-item">
                    <div class="buttons">

                        <a class="button is-primary is-small" href="{{ path('account_profile') }}">
                            <span class="icon"><i class="fas fa-user-circle"></i></span>
                            <span><strong>{{ app.user.username }}</strong></span>

                        </a>
                        <a class="button is-light is-small" href="{{ path('app_logout') }}">
                            <i class="fas fa-sign-out-alt"></i>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    {# Bloc qui contiendra le contenu de chaque page #}
    {% block content %}{% endblock %}

{% endblock %}